import { Typography, Box } from "@mui/material";

function Groups() {
	return (
		<Box sx={{ my: "3rem" }}>
			<Typography variant="h3">Groups</Typography>
			<Typography variant="Caption">Will be released soon</Typography>
		</Box>
	);
}

export default Groups;
